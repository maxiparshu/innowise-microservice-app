spring:
  datasource:
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate

  liquibase:
    enabled: true


  cache:
    type: redis

logging:
  level:
    org.springframework: INFO