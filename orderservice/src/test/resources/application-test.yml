spring:
  datasource:
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate

  liquibase:
    enabled: true
  cloud:
    discovery:
      enabled: false
logging:
  level:
    org.springframework: INFO


eureka:
  client:
    enabled: false
